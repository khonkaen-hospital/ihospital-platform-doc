# <ชื่อ integration> — API Specification (เต็ม)

ถอดจากต้นฉบับ `source/<ชื่อไฟล์>` (เวอร์ชัน · วันที่)
อ่านสรุป + ข้อควรระวังก่อนที่ [`README.md`](README.md)

- **Base URL:** `<base_url>`
- **Auth:** `<header / scheme>`
- **มาตรฐานข้อมูล:** `<เช่น HL7 FHIR / custom>`

---

## ภาพรวม API

| # | API | Method | Endpoint | ทิศทาง |
|---|---|---|---|---|
| 1 | <ชื่อ> | <GET/POST> | `<path>` | <push/pull> |

---

# API 1 — <ชื่อ>

<คำอธิบาย>

- **Method:** `<METHOD>`
- **Endpoint:** `{{base_url}}<path>`

## Parameters

| Parameter | Type | Required | Description | Level |
|---|---|:--:|---|:--:|
| | | | | |

## ตัวอย่าง request

```json
```

## ตัวอย่าง response

```json
```

---

<!-- ทำซ้ำหัวข้อ API ต่อ ๆ ไปตามจำนวน endpoint -->

# หมายเหตุเพิ่มเติม / Encryption / Error codes

<ถ้ามี>
